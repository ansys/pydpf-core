"""
matrix_product

Autogenerated DPF operator classes.
"""

from __future__ import annotations
from typing import TYPE_CHECKING

from warnings import warn
from ansys.dpf.core.dpf_operator import Operator
from ansys.dpf.core.inputs import Input, _Inputs
from ansys.dpf.core.outputs import Output, _Outputs
from ansys.dpf.core.operators.specification import PinSpecification, Specification
from ansys.dpf.core.config import Config
from ansys.dpf.core.server_types import AnyServerType

if TYPE_CHECKING:
    from ansys.dpf.core.field import Field
    from ansys.dpf.core.fields_container import FieldsContainer


class matrix_product(Operator):
    r"""Computes the product of two matrix or matrix-vector fields:

    +-----------------+-----------------+------------------------------------------------------------------+-----------------+
    | Pin 0           | Pin 1           | Operation                                                        | Result          |
    +=================+=================+==================================================================+=================+
    | matrix field    | matrix field    | `matrix                                                          | matrix field    |
    |                 |                 | product <https://en.wikipedia.org/wiki/Matrix_multiplication>`__ |                 |
    +-----------------+-----------------+------------------------------------------------------------------+-----------------+
    | matrix field    | vector field    | matrix-vector product                                            | vector field    |
    +-----------------+-----------------+------------------------------------------------------------------+-----------------+

    Raises an error if neither input is a matrix (second-order tensor)
    field, or if an input is neither a matrix nor a vector. For general
    inner products - including dot products and scaling - use the
    ``generalized_inner_product`` operator instead. If either input is
    empty, a dimensionless zero scalar field is returned.


    Inputs
    ------
    fieldA: Field or FieldsContainer or float
        Matrix field A (second-order tensor). Can also be a vector when pin 1 is the matrix.
    fieldB: Field or FieldsContainer or float
        Matrix field B (second-order tensor) or vector field B.

    Outputs
    -------
    field: Field
        Result of the matrix product; a matrix field or a vector field depending on the inputs.

    Examples
    --------
    >>> from ansys.dpf import core as dpf

    >>> # Instantiate operator
    >>> op = dpf.operators.math.matrix_product()

    >>> # Make input connections
    >>> my_fieldA = dpf.Field()
    >>> op.inputs.fieldA.connect(my_fieldA)
    >>> my_fieldB = dpf.Field()
    >>> op.inputs.fieldB.connect(my_fieldB)

    >>> # Instantiate operator and connect inputs in one line
    >>> op = dpf.operators.math.matrix_product(
    ...     fieldA=my_fieldA,
    ...     fieldB=my_fieldB,
    ... )

    >>> # Get output data
    >>> result_field = op.outputs.field()
    """

    def __init__(self, fieldA=None, fieldB=None, config=None, server=None):
        super().__init__(
            name="matrix_product",
            config=config,
            server=server,
            inputs_type=InputsMatrixProduct,
            outputs_type=OutputsMatrixProduct,
        )
        if fieldA is not None:
            self.inputs.fieldA.connect(fieldA)
        if fieldB is not None:
            self.inputs.fieldB.connect(fieldB)

    @staticmethod
    def _spec() -> Specification:
        description = r"""Computes the product of two matrix or matrix-vector fields:

+-----------------+-----------------+------------------------------------------------------------------+-----------------+
| Pin 0           | Pin 1           | Operation                                                        | Result          |
+=================+=================+==================================================================+=================+
| matrix field    | matrix field    | `matrix                                                          | matrix field    |
|                 |                 | product <https://en.wikipedia.org/wiki/Matrix_multiplication>`__ |                 |
+-----------------+-----------------+------------------------------------------------------------------+-----------------+
| matrix field    | vector field    | matrix-vector product                                            | vector field    |
+-----------------+-----------------+------------------------------------------------------------------+-----------------+

Raises an error if neither input is a matrix (second-order tensor)
field, or if an input is neither a matrix nor a vector. For general
inner products - including dot products and scaling - use the
``generalized_inner_product`` operator instead. If either input is
empty, a dimensionless zero scalar field is returned.
"""
        spec = Specification(
            description=description,
            map_input_pin_spec={
                0: PinSpecification(
                    name="fieldA",
                    type_names=[
                        "field",
                        "fields_container",
                        "double",
                        "vector<double>",
                    ],
                    optional=False,
                    document=r"""Matrix field A (second-order tensor). Can also be a vector when pin 1 is the matrix.""",
                ),
                1: PinSpecification(
                    name="fieldB",
                    type_names=[
                        "field",
                        "fields_container",
                        "double",
                        "vector<double>",
                    ],
                    optional=False,
                    document=r"""Matrix field B (second-order tensor) or vector field B.""",
                ),
            },
            map_output_pin_spec={
                0: PinSpecification(
                    name="field",
                    type_names=["field"],
                    optional=False,
                    document=r"""Result of the matrix product; a matrix field or a vector field depending on the inputs.""",
                ),
            },
        )
        return spec

    @staticmethod
    def default_config(server: AnyServerType = None) -> Config:
        """Returns the default config of the operator.

        This config can then be changed to the user needs and be used to
        instantiate the operator. The Configuration allows to customize
        how the operation will be processed by the operator.

        Parameters
        ----------
        server:
            Server with channel connected to the remote or local instance. When
            ``None``, attempts to use the global server.

        Returns
        -------
        config:
            A new Config instance equivalent to the default config for this operator.
        """
        return Operator.default_config(name="matrix_product", server=server)

    @property
    def inputs(self) -> InputsMatrixProduct:
        """Enables to connect inputs to the operator

        Returns
        --------
        inputs:
            An instance of InputsMatrixProduct.
        """
        return self._inputs

    @property
    def outputs(self) -> OutputsMatrixProduct:
        """Enables to get outputs of the operator by evaluating it

        Returns
        --------
        outputs:
            An instance of OutputsMatrixProduct.
        """
        return self._outputs


class InputsMatrixProduct(_Inputs):
    """Intermediate class used to connect user inputs to
    matrix_product operator.

    Examples
    --------
    >>> from ansys.dpf import core as dpf
    >>> op = dpf.operators.math.matrix_product()
    >>> my_fieldA = dpf.Field()
    >>> op.inputs.fieldA.connect(my_fieldA)
    >>> my_fieldB = dpf.Field()
    >>> op.inputs.fieldB.connect(my_fieldB)
    """

    def __init__(self, op: Operator):
        super().__init__(matrix_product._spec().inputs, op)
        self._fieldA: Input[Field | FieldsContainer | float] = Input(
            matrix_product._spec().input_pin(0), 0, op, -1
        )
        self._inputs.append(self._fieldA)
        self._fieldB: Input[Field | FieldsContainer | float] = Input(
            matrix_product._spec().input_pin(1), 1, op, -1
        )
        self._inputs.append(self._fieldB)

    @property
    def fieldA(self) -> Input[Field | FieldsContainer | float]:
        r"""Allows to connect fieldA input to the operator.

        Matrix field A (second-order tensor). Can also be a vector when pin 1 is the matrix.

        Returns
        -------
        input:
            An Input instance for this pin.

        Examples
        --------
        >>> from ansys.dpf import core as dpf
        >>> op = dpf.operators.math.matrix_product()
        >>> op.inputs.fieldA.connect(my_fieldA)
        >>> # or
        >>> op.inputs.fieldA(my_fieldA)
        """
        return self._fieldA

    @property
    def fieldB(self) -> Input[Field | FieldsContainer | float]:
        r"""Allows to connect fieldB input to the operator.

        Matrix field B (second-order tensor) or vector field B.

        Returns
        -------
        input:
            An Input instance for this pin.

        Examples
        --------
        >>> from ansys.dpf import core as dpf
        >>> op = dpf.operators.math.matrix_product()
        >>> op.inputs.fieldB.connect(my_fieldB)
        >>> # or
        >>> op.inputs.fieldB(my_fieldB)
        """
        return self._fieldB


class OutputsMatrixProduct(_Outputs):
    """Intermediate class used to get outputs from
    matrix_product operator.

    Examples
    --------
    >>> from ansys.dpf import core as dpf
    >>> op = dpf.operators.math.matrix_product()
    >>> # Connect inputs : op.inputs. ...
    >>> result_field = op.outputs.field()
    """

    def __init__(self, op: Operator):
        super().__init__(matrix_product._spec().outputs, op)
        self._field: Output[Field] = Output(matrix_product._spec().output_pin(0), 0, op)
        self._outputs.append(self._field)

    @property
    def field(self) -> Output[Field]:
        r"""Allows to get field output of the operator

        Result of the matrix product; a matrix field or a vector field depending on the inputs.

        Returns
        -------
        output:
            An Output instance for this pin.

        Examples
        --------
        >>> from ansys.dpf import core as dpf
        >>> op = dpf.operators.math.matrix_product()
        >>> # Get the output from op.outputs. ...
        >>> result_field = op.outputs.field()
        """
        return self._field
