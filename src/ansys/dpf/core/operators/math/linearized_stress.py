"""
linearized_stress

Autogenerated DPF operator classes.
"""

from __future__ import annotations
from typing import TYPE_CHECKING

from warnings import warn
from ansys.dpf.core.dpf_operator import Operator
from ansys.dpf.core.inputs import Input, _Inputs
from ansys.dpf.core.outputs import Output, _Outputs
from ansys.dpf.core.operators.specification import PinSpecification, Specification
from ansys.dpf.core.config import Config
from ansys.dpf.core.server_types import AnyServerType

if TYPE_CHECKING:
    from ansys.dpf.core.field import Field
    from ansys.dpf.core.fields_container import FieldsContainer


class linearized_stress(Operator):
    r"""get linearized stress


    Inputs
    ------
    stress_comps: FieldsContainer
        Stress tensor components evaluated at path points
    iComp: int
        Stress component index to linearize
    path_coords: Field
        Path point coordinates
    rho: float
        Optional geometric parameter
    kbr: int
        Option flag controlling bending/reference behavior
    ksxbzro: int
        Option flag controlling coordinate/zero-reference handling
    out_type: int
        Requested linearized stress output type.

    Outputs
    -------
    linearized_stress_results: FieldsContainer
        Selected linearized stress results for the requested output type
    linearized_stress_all_results: FieldsContainer
        All linearized stress result types computed along the path
    linearized_stress_params: FieldsContainer
        Linearization parameters and metadata

    Examples
    --------
    >>> from ansys.dpf import core as dpf

    >>> # Instantiate operator
    >>> op = dpf.operators.math.linearized_stress()

    >>> # Make input connections
    >>> my_stress_comps = dpf.FieldsContainer()
    >>> op.inputs.stress_comps.connect(my_stress_comps)
    >>> my_iComp = int()
    >>> op.inputs.iComp.connect(my_iComp)
    >>> my_path_coords = dpf.Field()
    >>> op.inputs.path_coords.connect(my_path_coords)
    >>> my_rho = float()
    >>> op.inputs.rho.connect(my_rho)
    >>> my_kbr = int()
    >>> op.inputs.kbr.connect(my_kbr)
    >>> my_ksxbzro = int()
    >>> op.inputs.ksxbzro.connect(my_ksxbzro)
    >>> my_out_type = int()
    >>> op.inputs.out_type.connect(my_out_type)

    >>> # Instantiate operator and connect inputs in one line
    >>> op = dpf.operators.math.linearized_stress(
    ...     stress_comps=my_stress_comps,
    ...     iComp=my_iComp,
    ...     path_coords=my_path_coords,
    ...     rho=my_rho,
    ...     kbr=my_kbr,
    ...     ksxbzro=my_ksxbzro,
    ...     out_type=my_out_type,
    ... )

    >>> # Get output data
    >>> result_linearized_stress_results = op.outputs.linearized_stress_results()
    >>> result_linearized_stress_all_results = op.outputs.linearized_stress_all_results()
    >>> result_linearized_stress_params = op.outputs.linearized_stress_params()
    """

    def __init__(
        self,
        stress_comps=None,
        iComp=None,
        path_coords=None,
        rho=None,
        kbr=None,
        ksxbzro=None,
        out_type=None,
        config=None,
        server=None,
    ):
        super().__init__(
            name="mechanical::linearized_stress_op",
            config=config,
            server=server,
            inputs_type=InputsLinearizedStress,
            outputs_type=OutputsLinearizedStress,
        )
        if stress_comps is not None:
            self.inputs.stress_comps.connect(stress_comps)
        if iComp is not None:
            self.inputs.iComp.connect(iComp)
        if path_coords is not None:
            self.inputs.path_coords.connect(path_coords)
        if rho is not None:
            self.inputs.rho.connect(rho)
        if kbr is not None:
            self.inputs.kbr.connect(kbr)
        if ksxbzro is not None:
            self.inputs.ksxbzro.connect(ksxbzro)
        if out_type is not None:
            self.inputs.out_type.connect(out_type)

    @staticmethod
    def _spec() -> Specification:
        description = r"""get linearized stress
"""
        spec = Specification(
            description=description,
            map_input_pin_spec={
                0: PinSpecification(
                    name="stress_comps",
                    type_names=["fields_container"],
                    optional=False,
                    document=r"""Stress tensor components evaluated at path points""",
                ),
                1: PinSpecification(
                    name="iComp",
                    type_names=["int32"],
                    optional=False,
                    document=r"""Stress component index to linearize""",
                ),
                2: PinSpecification(
                    name="path_coords",
                    type_names=["field"],
                    optional=False,
                    document=r"""Path point coordinates""",
                ),
                3: PinSpecification(
                    name="rho",
                    type_names=["double"],
                    optional=False,
                    document=r"""Optional geometric parameter""",
                ),
                4: PinSpecification(
                    name="kbr",
                    type_names=["int32"],
                    optional=False,
                    document=r"""Option flag controlling bending/reference behavior""",
                ),
                5: PinSpecification(
                    name="ksxbzro",
                    type_names=["int32"],
                    optional=False,
                    document=r"""Option flag controlling coordinate/zero-reference handling""",
                ),
                6: PinSpecification(
                    name="out_type",
                    type_names=["int32"],
                    optional=False,
                    document=r"""Requested linearized stress output type.""",
                ),
            },
            map_output_pin_spec={
                0: PinSpecification(
                    name="linearized_stress_results",
                    type_names=["fields_container"],
                    optional=False,
                    document=r"""Selected linearized stress results for the requested output type""",
                ),
                1: PinSpecification(
                    name="linearized_stress_all_results",
                    type_names=["fields_container"],
                    optional=False,
                    document=r"""All linearized stress result types computed along the path""",
                ),
                2: PinSpecification(
                    name="linearized_stress_params",
                    type_names=["fields_container"],
                    optional=False,
                    document=r"""Linearization parameters and metadata""",
                ),
            },
        )
        return spec

    @staticmethod
    def default_config(server: AnyServerType = None) -> Config:
        """Returns the default config of the operator.

        This config can then be changed to the user needs and be used to
        instantiate the operator. The Configuration allows to customize
        how the operation will be processed by the operator.

        Parameters
        ----------
        server:
            Server with channel connected to the remote or local instance. When
            ``None``, attempts to use the global server.

        Returns
        -------
        config:
            A new Config instance equivalent to the default config for this operator.
        """
        return Operator.default_config(
            name="mechanical::linearized_stress_op", server=server
        )

    @property
    def inputs(self) -> InputsLinearizedStress:
        """Enables to connect inputs to the operator

        Returns
        --------
        inputs:
            An instance of InputsLinearizedStress.
        """
        return self._inputs

    @property
    def outputs(self) -> OutputsLinearizedStress:
        """Enables to get outputs of the operator by evaluating it

        Returns
        --------
        outputs:
            An instance of OutputsLinearizedStress.
        """
        return self._outputs


class InputsLinearizedStress(_Inputs):
    """Intermediate class used to connect user inputs to
    linearized_stress operator.

    Examples
    --------
    >>> from ansys.dpf import core as dpf
    >>> op = dpf.operators.math.linearized_stress()
    >>> my_stress_comps = dpf.FieldsContainer()
    >>> op.inputs.stress_comps.connect(my_stress_comps)
    >>> my_iComp = int()
    >>> op.inputs.iComp.connect(my_iComp)
    >>> my_path_coords = dpf.Field()
    >>> op.inputs.path_coords.connect(my_path_coords)
    >>> my_rho = float()
    >>> op.inputs.rho.connect(my_rho)
    >>> my_kbr = int()
    >>> op.inputs.kbr.connect(my_kbr)
    >>> my_ksxbzro = int()
    >>> op.inputs.ksxbzro.connect(my_ksxbzro)
    >>> my_out_type = int()
    >>> op.inputs.out_type.connect(my_out_type)
    """

    def __init__(self, op: Operator):
        super().__init__(linearized_stress._spec().inputs, op)
        self._stress_comps: Input[FieldsContainer] = Input(
            linearized_stress._spec().input_pin(0), 0, op, -1
        )
        self._inputs.append(self._stress_comps)
        self._iComp: Input[int] = Input(
            linearized_stress._spec().input_pin(1), 1, op, -1
        )
        self._inputs.append(self._iComp)
        self._path_coords: Input[Field] = Input(
            linearized_stress._spec().input_pin(2), 2, op, -1
        )
        self._inputs.append(self._path_coords)
        self._rho: Input[float] = Input(
            linearized_stress._spec().input_pin(3), 3, op, -1
        )
        self._inputs.append(self._rho)
        self._kbr: Input[int] = Input(linearized_stress._spec().input_pin(4), 4, op, -1)
        self._inputs.append(self._kbr)
        self._ksxbzro: Input[int] = Input(
            linearized_stress._spec().input_pin(5), 5, op, -1
        )
        self._inputs.append(self._ksxbzro)
        self._out_type: Input[int] = Input(
            linearized_stress._spec().input_pin(6), 6, op, -1
        )
        self._inputs.append(self._out_type)

    @property
    def stress_comps(self) -> Input[FieldsContainer]:
        r"""Allows to connect stress_comps input to the operator.

        Stress tensor components evaluated at path points

        Returns
        -------
        input:
            An Input instance for this pin.

        Examples
        --------
        >>> from ansys.dpf import core as dpf
        >>> op = dpf.operators.math.linearized_stress()
        >>> op.inputs.stress_comps.connect(my_stress_comps)
        >>> # or
        >>> op.inputs.stress_comps(my_stress_comps)
        """
        return self._stress_comps

    @property
    def iComp(self) -> Input[int]:
        r"""Allows to connect iComp input to the operator.

        Stress component index to linearize

        Returns
        -------
        input:
            An Input instance for this pin.

        Examples
        --------
        >>> from ansys.dpf import core as dpf
        >>> op = dpf.operators.math.linearized_stress()
        >>> op.inputs.iComp.connect(my_iComp)
        >>> # or
        >>> op.inputs.iComp(my_iComp)
        """
        return self._iComp

    @property
    def path_coords(self) -> Input[Field]:
        r"""Allows to connect path_coords input to the operator.

        Path point coordinates

        Returns
        -------
        input:
            An Input instance for this pin.

        Examples
        --------
        >>> from ansys.dpf import core as dpf
        >>> op = dpf.operators.math.linearized_stress()
        >>> op.inputs.path_coords.connect(my_path_coords)
        >>> # or
        >>> op.inputs.path_coords(my_path_coords)
        """
        return self._path_coords

    @property
    def rho(self) -> Input[float]:
        r"""Allows to connect rho input to the operator.

        Optional geometric parameter

        Returns
        -------
        input:
            An Input instance for this pin.

        Examples
        --------
        >>> from ansys.dpf import core as dpf
        >>> op = dpf.operators.math.linearized_stress()
        >>> op.inputs.rho.connect(my_rho)
        >>> # or
        >>> op.inputs.rho(my_rho)
        """
        return self._rho

    @property
    def kbr(self) -> Input[int]:
        r"""Allows to connect kbr input to the operator.

        Option flag controlling bending/reference behavior

        Returns
        -------
        input:
            An Input instance for this pin.

        Examples
        --------
        >>> from ansys.dpf import core as dpf
        >>> op = dpf.operators.math.linearized_stress()
        >>> op.inputs.kbr.connect(my_kbr)
        >>> # or
        >>> op.inputs.kbr(my_kbr)
        """
        return self._kbr

    @property
    def ksxbzro(self) -> Input[int]:
        r"""Allows to connect ksxbzro input to the operator.

        Option flag controlling coordinate/zero-reference handling

        Returns
        -------
        input:
            An Input instance for this pin.

        Examples
        --------
        >>> from ansys.dpf import core as dpf
        >>> op = dpf.operators.math.linearized_stress()
        >>> op.inputs.ksxbzro.connect(my_ksxbzro)
        >>> # or
        >>> op.inputs.ksxbzro(my_ksxbzro)
        """
        return self._ksxbzro

    @property
    def out_type(self) -> Input[int]:
        r"""Allows to connect out_type input to the operator.

        Requested linearized stress output type.

        Returns
        -------
        input:
            An Input instance for this pin.

        Examples
        --------
        >>> from ansys.dpf import core as dpf
        >>> op = dpf.operators.math.linearized_stress()
        >>> op.inputs.out_type.connect(my_out_type)
        >>> # or
        >>> op.inputs.out_type(my_out_type)
        """
        return self._out_type


class OutputsLinearizedStress(_Outputs):
    """Intermediate class used to get outputs from
    linearized_stress operator.

    Examples
    --------
    >>> from ansys.dpf import core as dpf
    >>> op = dpf.operators.math.linearized_stress()
    >>> # Connect inputs : op.inputs. ...
    >>> result_linearized_stress_results = op.outputs.linearized_stress_results()
    >>> result_linearized_stress_all_results = op.outputs.linearized_stress_all_results()
    >>> result_linearized_stress_params = op.outputs.linearized_stress_params()
    """

    def __init__(self, op: Operator):
        super().__init__(linearized_stress._spec().outputs, op)
        self._linearized_stress_results: Output[FieldsContainer] = Output(
            linearized_stress._spec().output_pin(0), 0, op
        )
        self._outputs.append(self._linearized_stress_results)
        self._linearized_stress_all_results: Output[FieldsContainer] = Output(
            linearized_stress._spec().output_pin(1), 1, op
        )
        self._outputs.append(self._linearized_stress_all_results)
        self._linearized_stress_params: Output[FieldsContainer] = Output(
            linearized_stress._spec().output_pin(2), 2, op
        )
        self._outputs.append(self._linearized_stress_params)

    @property
    def linearized_stress_results(self) -> Output[FieldsContainer]:
        r"""Allows to get linearized_stress_results output of the operator

        Selected linearized stress results for the requested output type

        Returns
        -------
        output:
            An Output instance for this pin.

        Examples
        --------
        >>> from ansys.dpf import core as dpf
        >>> op = dpf.operators.math.linearized_stress()
        >>> # Get the output from op.outputs. ...
        >>> result_linearized_stress_results = op.outputs.linearized_stress_results()
        """
        return self._linearized_stress_results

    @property
    def linearized_stress_all_results(self) -> Output[FieldsContainer]:
        r"""Allows to get linearized_stress_all_results output of the operator

        All linearized stress result types computed along the path

        Returns
        -------
        output:
            An Output instance for this pin.

        Examples
        --------
        >>> from ansys.dpf import core as dpf
        >>> op = dpf.operators.math.linearized_stress()
        >>> # Get the output from op.outputs. ...
        >>> result_linearized_stress_all_results = op.outputs.linearized_stress_all_results()
        """
        return self._linearized_stress_all_results

    @property
    def linearized_stress_params(self) -> Output[FieldsContainer]:
        r"""Allows to get linearized_stress_params output of the operator

        Linearization parameters and metadata

        Returns
        -------
        output:
            An Output instance for this pin.

        Examples
        --------
        >>> from ansys.dpf import core as dpf
        >>> op = dpf.operators.math.linearized_stress()
        >>> # Get the output from op.outputs. ...
        >>> result_linearized_stress_params = op.outputs.linearized_stress_params()
        """
        return self._linearized_stress_params
