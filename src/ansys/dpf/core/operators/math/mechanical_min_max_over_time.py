"""
mechanical_min_max_over_time

Autogenerated DPF operator classes.
"""

from __future__ import annotations
from typing import TYPE_CHECKING

from warnings import warn
from ansys.dpf.core.dpf_operator import Operator
from ansys.dpf.core.inputs import Input, _Inputs
from ansys.dpf.core.outputs import Output, _Outputs
from ansys.dpf.core.operators.specification import PinSpecification, Specification
from ansys.dpf.core.config import Config
from ansys.dpf.core.server_types import AnyServerType

if TYPE_CHECKING:
    from ansys.dpf.core.fields_container import FieldsContainer


class mechanical_min_max_over_time(Operator):
    r"""Dispatch operator that selects and runs a minimum/maximum operator over
    time or frequency, based on the integer selector on pin 5.

    Selector values on pin 5:

    - ``0``: ``min_max_by_time`` - per-step, per-component minimum and
      maximum, aggregating every entity of each field (elemental-nodal
      values collapse into the reduction).
    - ``1``: ``max_over_time_by_entity`` - per-entity, per-component maximum
      across all time or frequency steps.
    - ``2``: ``time_of_max_by_entity`` - time or frequency at which each
      per-entity, per-component maximum occurs.
    - ``7``: ``min_over_time_by_entity`` - per-entity, per-component minimum
      across all time or frequency steps.
    - ``8``: ``time_of_min_by_entity`` - time or frequency at which each
      per-entity, per-component minimum occurs.

    Selectors 1, 2, 7 and 8 keep the entity axis: the underlying operator
    (``min_max_over_time_by_entity``) returns one value per entity, per
    component and per shell layer when available. Selector 0
    (``min_max_by_time``) reduces across entities instead.

    Output pin 0 holds the primary result of the selected operator. Output
    pin 1 is optional and populated only when the selected operator produces
    two outputs (currently only selector ``0``).


    Inputs
    ------
    fields_container: FieldsContainer
        Input fields container passed through to the selected underlying operator.
    int32: int
        Selector integer that chooses the underlying operator. Supported values: `0` (min_max_by_time), `1` (max_over_time_by_entity), `2` (time_of_max_by_entity), `7` (min_over_time_by_entity), `8` (time_of_min_by_entity).

    Outputs
    -------
    field_container_1: FieldsContainer
        Primary result forwarded from the selected underlying operator.
    field_container_2: FieldsContainer, optional
        Secondary result forwarded from the selected underlying operator. Populated only when the underlying operator produces two outputs (currently only selector `0` populates this pin).

    Examples
    --------
    >>> from ansys.dpf import core as dpf

    >>> # Instantiate operator
    >>> op = dpf.operators.math.mechanical_min_max_over_time()

    >>> # Make input connections
    >>> my_fields_container = dpf.FieldsContainer()
    >>> op.inputs.fields_container.connect(my_fields_container)
    >>> my_int32 = int()
    >>> op.inputs.int32.connect(my_int32)

    >>> # Instantiate operator and connect inputs in one line
    >>> op = dpf.operators.math.mechanical_min_max_over_time(
    ...     fields_container=my_fields_container,
    ...     int32=my_int32,
    ... )

    >>> # Get output data
    >>> result_field_container_1 = op.outputs.field_container_1()
    >>> result_field_container_2 = op.outputs.field_container_2()
    """

    def __init__(self, fields_container=None, int32=None, config=None, server=None):
        super().__init__(
            name="mechanical::min_max_over_time",
            config=config,
            server=server,
            inputs_type=InputsMechanicalMinMaxOverTime,
            outputs_type=OutputsMechanicalMinMaxOverTime,
        )
        if fields_container is not None:
            self.inputs.fields_container.connect(fields_container)
        if int32 is not None:
            self.inputs.int32.connect(int32)

    @staticmethod
    def _spec() -> Specification:
        description = r"""Dispatch operator that selects and runs a minimum/maximum operator over
time or frequency, based on the integer selector on pin 5.

Selector values on pin 5:

- ``0``: ``min_max_by_time`` - per-step, per-component minimum and
  maximum, aggregating every entity of each field (elemental-nodal
  values collapse into the reduction).
- ``1``: ``max_over_time_by_entity`` - per-entity, per-component maximum
  across all time or frequency steps.
- ``2``: ``time_of_max_by_entity`` - time or frequency at which each
  per-entity, per-component maximum occurs.
- ``7``: ``min_over_time_by_entity`` - per-entity, per-component minimum
  across all time or frequency steps.
- ``8``: ``time_of_min_by_entity`` - time or frequency at which each
  per-entity, per-component minimum occurs.

Selectors 1, 2, 7 and 8 keep the entity axis: the underlying operator
(``min_max_over_time_by_entity``) returns one value per entity, per
component and per shell layer when available. Selector 0
(``min_max_by_time``) reduces across entities instead.

Output pin 0 holds the primary result of the selected operator. Output
pin 1 is optional and populated only when the selected operator produces
two outputs (currently only selector ``0``).
"""
        spec = Specification(
            description=description,
            map_input_pin_spec={
                0: PinSpecification(
                    name="fields_container",
                    type_names=["fields_container"],
                    optional=False,
                    document=r"""Input fields container passed through to the selected underlying operator.""",
                ),
                5: PinSpecification(
                    name="int32",
                    type_names=["int32"],
                    optional=False,
                    document=r"""Selector integer that chooses the underlying operator. Supported values: `0` (min_max_by_time), `1` (max_over_time_by_entity), `2` (time_of_max_by_entity), `7` (min_over_time_by_entity), `8` (time_of_min_by_entity).""",
                ),
            },
            map_output_pin_spec={
                0: PinSpecification(
                    name="field_container_1",
                    type_names=["fields_container"],
                    optional=False,
                    document=r"""Primary result forwarded from the selected underlying operator.""",
                ),
                1: PinSpecification(
                    name="field_container_2",
                    type_names=["fields_container"],
                    optional=True,
                    document=r"""Secondary result forwarded from the selected underlying operator. Populated only when the underlying operator produces two outputs (currently only selector `0` populates this pin).""",
                ),
            },
        )
        return spec

    @staticmethod
    def default_config(server: AnyServerType = None) -> Config:
        """Returns the default config of the operator.

        This config can then be changed to the user needs and be used to
        instantiate the operator. The Configuration allows to customize
        how the operation will be processed by the operator.

        Parameters
        ----------
        server:
            Server with channel connected to the remote or local instance. When
            ``None``, attempts to use the global server.

        Returns
        -------
        config:
            A new Config instance equivalent to the default config for this operator.
        """
        return Operator.default_config(
            name="mechanical::min_max_over_time", server=server
        )

    @property
    def inputs(self) -> InputsMechanicalMinMaxOverTime:
        """Enables to connect inputs to the operator

        Returns
        --------
        inputs:
            An instance of InputsMechanicalMinMaxOverTime.
        """
        return self._inputs

    @property
    def outputs(self) -> OutputsMechanicalMinMaxOverTime:
        """Enables to get outputs of the operator by evaluating it

        Returns
        --------
        outputs:
            An instance of OutputsMechanicalMinMaxOverTime.
        """
        return self._outputs


class InputsMechanicalMinMaxOverTime(_Inputs):
    """Intermediate class used to connect user inputs to
    mechanical_min_max_over_time operator.

    Examples
    --------
    >>> from ansys.dpf import core as dpf
    >>> op = dpf.operators.math.mechanical_min_max_over_time()
    >>> my_fields_container = dpf.FieldsContainer()
    >>> op.inputs.fields_container.connect(my_fields_container)
    >>> my_int32 = int()
    >>> op.inputs.int32.connect(my_int32)
    """

    def __init__(self, op: Operator):
        super().__init__(mechanical_min_max_over_time._spec().inputs, op)
        self._fields_container: Input[FieldsContainer] = Input(
            mechanical_min_max_over_time._spec().input_pin(0), 0, op, -1
        )
        self._inputs.append(self._fields_container)
        self._int32: Input[int] = Input(
            mechanical_min_max_over_time._spec().input_pin(5), 5, op, -1
        )
        self._inputs.append(self._int32)

    @property
    def fields_container(self) -> Input[FieldsContainer]:
        r"""Allows to connect fields_container input to the operator.

        Input fields container passed through to the selected underlying operator.

        Returns
        -------
        input:
            An Input instance for this pin.

        Examples
        --------
        >>> from ansys.dpf import core as dpf
        >>> op = dpf.operators.math.mechanical_min_max_over_time()
        >>> op.inputs.fields_container.connect(my_fields_container)
        >>> # or
        >>> op.inputs.fields_container(my_fields_container)
        """
        return self._fields_container

    @property
    def int32(self) -> Input[int]:
        r"""Allows to connect int32 input to the operator.

        Selector integer that chooses the underlying operator. Supported values: `0` (min_max_by_time), `1` (max_over_time_by_entity), `2` (time_of_max_by_entity), `7` (min_over_time_by_entity), `8` (time_of_min_by_entity).

        Returns
        -------
        input:
            An Input instance for this pin.

        Examples
        --------
        >>> from ansys.dpf import core as dpf
        >>> op = dpf.operators.math.mechanical_min_max_over_time()
        >>> op.inputs.int32.connect(my_int32)
        >>> # or
        >>> op.inputs.int32(my_int32)
        """
        return self._int32


class OutputsMechanicalMinMaxOverTime(_Outputs):
    """Intermediate class used to get outputs from
    mechanical_min_max_over_time operator.

    Examples
    --------
    >>> from ansys.dpf import core as dpf
    >>> op = dpf.operators.math.mechanical_min_max_over_time()
    >>> # Connect inputs : op.inputs. ...
    >>> result_field_container_1 = op.outputs.field_container_1()
    >>> result_field_container_2 = op.outputs.field_container_2()
    """

    def __init__(self, op: Operator):
        super().__init__(mechanical_min_max_over_time._spec().outputs, op)
        self._field_container_1: Output[FieldsContainer] = Output(
            mechanical_min_max_over_time._spec().output_pin(0), 0, op
        )
        self._outputs.append(self._field_container_1)
        self._field_container_2: Output[FieldsContainer] = Output(
            mechanical_min_max_over_time._spec().output_pin(1), 1, op
        )
        self._outputs.append(self._field_container_2)

    @property
    def field_container_1(self) -> Output[FieldsContainer]:
        r"""Allows to get field_container_1 output of the operator

        Primary result forwarded from the selected underlying operator.

        Returns
        -------
        output:
            An Output instance for this pin.

        Examples
        --------
        >>> from ansys.dpf import core as dpf
        >>> op = dpf.operators.math.mechanical_min_max_over_time()
        >>> # Get the output from op.outputs. ...
        >>> result_field_container_1 = op.outputs.field_container_1()
        """
        return self._field_container_1

    @property
    def field_container_2(self) -> Output[FieldsContainer]:
        r"""Allows to get field_container_2 output of the operator

        Secondary result forwarded from the selected underlying operator. Populated only when the underlying operator produces two outputs (currently only selector `0` populates this pin).

        Returns
        -------
        output:
            An Output instance for this pin.

        Examples
        --------
        >>> from ansys.dpf import core as dpf
        >>> op = dpf.operators.math.mechanical_min_max_over_time()
        >>> # Get the output from op.outputs. ...
        >>> result_field_container_2 = op.outputs.field_container_2()
        """
        return self._field_container_2
