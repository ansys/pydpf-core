"""
edge_decimation

Autogenerated DPF operator classes.
"""

from __future__ import annotations
from typing import TYPE_CHECKING

from warnings import warn
from ansys.dpf.core.dpf_operator import Operator
from ansys.dpf.core.inputs import Input, _Inputs
from ansys.dpf.core.outputs import Output, _Outputs
from ansys.dpf.core.operators.specification import PinSpecification, Specification
from ansys.dpf.core.config import Config
from ansys.dpf.core.server_types import AnyServerType

if TYPE_CHECKING:
    from ansys.dpf.core.meshed_region import MeshedRegion


class edge_decimation(Operator):
    r"""Takes a wireframe mesh (line elements) and reduces its node and edge
    count by collapsing interior nodes whose two incident edges deviate from
    straight by less than the given angular threshold. Branch nodes and
    sharp corners are preserved.


    Inputs
    ------
    mesh: MeshedRegion
    angular_limit_in_radian: float
        angle threshold in radian that will trigger an edge removal.

    Outputs
    -------
    wireframe: MeshedRegion

    Examples
    --------
    >>> from ansys.dpf import core as dpf

    >>> # Instantiate operator
    >>> op = dpf.operators.mesh.edge_decimation()

    >>> # Make input connections
    >>> my_mesh = dpf.MeshedRegion()
    >>> op.inputs.mesh.connect(my_mesh)
    >>> my_angular_limit_in_radian = float()
    >>> op.inputs.angular_limit_in_radian.connect(my_angular_limit_in_radian)

    >>> # Instantiate operator and connect inputs in one line
    >>> op = dpf.operators.mesh.edge_decimation(
    ...     mesh=my_mesh,
    ...     angular_limit_in_radian=my_angular_limit_in_radian,
    ... )

    >>> # Get output data
    >>> result_wireframe = op.outputs.wireframe()
    """

    def __init__(
        self, mesh=None, angular_limit_in_radian=None, config=None, server=None
    ):
        super().__init__(
            name="edge_decimation",
            config=config,
            server=server,
            inputs_type=InputsEdgeDecimation,
            outputs_type=OutputsEdgeDecimation,
        )
        if mesh is not None:
            self.inputs.mesh.connect(mesh)
        if angular_limit_in_radian is not None:
            self.inputs.angular_limit_in_radian.connect(angular_limit_in_radian)

    @staticmethod
    def _spec() -> Specification:
        description = r"""Takes a wireframe mesh (line elements) and reduces its node and edge
count by collapsing interior nodes whose two incident edges deviate from
straight by less than the given angular threshold. Branch nodes and
sharp corners are preserved.
"""
        spec = Specification(
            description=description,
            map_input_pin_spec={
                0: PinSpecification(
                    name="mesh",
                    type_names=["abstract_meshed_region"],
                    optional=False,
                    document=r"""""",
                ),
                1: PinSpecification(
                    name="angular_limit_in_radian",
                    type_names=["double"],
                    optional=False,
                    document=r"""angle threshold in radian that will trigger an edge removal.""",
                ),
            },
            map_output_pin_spec={
                0: PinSpecification(
                    name="wireframe",
                    type_names=["abstract_meshed_region"],
                    optional=False,
                    document=r"""""",
                ),
            },
        )
        return spec

    @staticmethod
    def default_config(server: AnyServerType = None) -> Config:
        """Returns the default config of the operator.

        This config can then be changed to the user needs and be used to
        instantiate the operator. The Configuration allows to customize
        how the operation will be processed by the operator.

        Parameters
        ----------
        server:
            Server with channel connected to the remote or local instance. When
            ``None``, attempts to use the global server.

        Returns
        -------
        config:
            A new Config instance equivalent to the default config for this operator.
        """
        return Operator.default_config(name="edge_decimation", server=server)

    @property
    def inputs(self) -> InputsEdgeDecimation:
        """Enables to connect inputs to the operator

        Returns
        --------
        inputs:
            An instance of InputsEdgeDecimation.
        """
        return self._inputs

    @property
    def outputs(self) -> OutputsEdgeDecimation:
        """Enables to get outputs of the operator by evaluating it

        Returns
        --------
        outputs:
            An instance of OutputsEdgeDecimation.
        """
        return self._outputs


class InputsEdgeDecimation(_Inputs):
    """Intermediate class used to connect user inputs to
    edge_decimation operator.

    Examples
    --------
    >>> from ansys.dpf import core as dpf
    >>> op = dpf.operators.mesh.edge_decimation()
    >>> my_mesh = dpf.MeshedRegion()
    >>> op.inputs.mesh.connect(my_mesh)
    >>> my_angular_limit_in_radian = float()
    >>> op.inputs.angular_limit_in_radian.connect(my_angular_limit_in_radian)
    """

    def __init__(self, op: Operator):
        super().__init__(edge_decimation._spec().inputs, op)
        self._mesh: Input[MeshedRegion] = Input(
            edge_decimation._spec().input_pin(0), 0, op, -1
        )
        self._inputs.append(self._mesh)
        self._angular_limit_in_radian: Input[float] = Input(
            edge_decimation._spec().input_pin(1), 1, op, -1
        )
        self._inputs.append(self._angular_limit_in_radian)

    @property
    def mesh(self) -> Input[MeshedRegion]:
        r"""Allows to connect mesh input to the operator.

        Returns
        -------
        input:
            An Input instance for this pin.

        Examples
        --------
        >>> from ansys.dpf import core as dpf
        >>> op = dpf.operators.mesh.edge_decimation()
        >>> op.inputs.mesh.connect(my_mesh)
        >>> # or
        >>> op.inputs.mesh(my_mesh)
        """
        return self._mesh

    @property
    def angular_limit_in_radian(self) -> Input[float]:
        r"""Allows to connect angular_limit_in_radian input to the operator.

        angle threshold in radian that will trigger an edge removal.

        Returns
        -------
        input:
            An Input instance for this pin.

        Examples
        --------
        >>> from ansys.dpf import core as dpf
        >>> op = dpf.operators.mesh.edge_decimation()
        >>> op.inputs.angular_limit_in_radian.connect(my_angular_limit_in_radian)
        >>> # or
        >>> op.inputs.angular_limit_in_radian(my_angular_limit_in_radian)
        """
        return self._angular_limit_in_radian


class OutputsEdgeDecimation(_Outputs):
    """Intermediate class used to get outputs from
    edge_decimation operator.

    Examples
    --------
    >>> from ansys.dpf import core as dpf
    >>> op = dpf.operators.mesh.edge_decimation()
    >>> # Connect inputs : op.inputs. ...
    >>> result_wireframe = op.outputs.wireframe()
    """

    def __init__(self, op: Operator):
        super().__init__(edge_decimation._spec().outputs, op)
        self._wireframe: Output[MeshedRegion] = Output(
            edge_decimation._spec().output_pin(0), 0, op
        )
        self._outputs.append(self._wireframe)

    @property
    def wireframe(self) -> Output[MeshedRegion]:
        r"""Allows to get wireframe output of the operator

        Returns
        -------
        output:
            An Output instance for this pin.

        Examples
        --------
        >>> from ansys.dpf import core as dpf
        >>> op = dpf.operators.mesh.edge_decimation()
        >>> # Get the output from op.outputs. ...
        >>> result_wireframe = op.outputs.wireframe()
        """
        return self._wireframe
