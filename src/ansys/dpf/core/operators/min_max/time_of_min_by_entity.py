"""
time_of_min_by_entity

Autogenerated DPF operator classes.
"""

from __future__ import annotations
from typing import TYPE_CHECKING

from warnings import warn
from ansys.dpf.core.dpf_operator import Operator
from ansys.dpf.core.inputs import Input, _Inputs
from ansys.dpf.core.outputs import Output, _Outputs
from ansys.dpf.core.operators.specification import PinSpecification, Specification
from ansys.dpf.core.config import Config
from ansys.dpf.core.server_types import AnyServerType

if TYPE_CHECKING:
    from ansys.dpf.core.fields_container import FieldsContainer


class time_of_min_by_entity(Operator):
    r"""Thin wrapper around ``min_max_over_time_by_entity`` that exposes only
    the time or frequency value at which each per-entity, per-component
    minimum occurred.

    The result forwarded on output pin 0 is pin 2 of
    ``min_max_over_time_by_entity`` (the ``time_freq_of_min`` fields
    container). It is populated only when the input carries a time-frequency
    support.

    **When to use:** you only need the time of minimum. Prefer
    ``min_max_over_time_by_entity`` directly when you also need the extremum
    value or the maximum-side outputs.


    Inputs
    ------
    fields_container: FieldsContainer
        Fields container aggregated per entity across all time or frequency steps. Must expose the `time` label; otherwise the input is forwarded unchanged by the underlying operator.
    abs_value: bool, optional
        When `true`, absolute values of the field entries are used before the min is computed. Default: `false`.
    compute_amplitude: bool, optional
        When `true` and the input fields container has the `complex` label, the amplitude of the complex values is used before the min is computed. Ignored otherwise. Default: `false`.

    Outputs
    -------
    fields_container: FieldsContainer
        Time or frequency at which each per-entity, per-component minimum occurred. Populated only when the input carries a time-frequency support. Same shape as pin 2 of `min_max_over_time_by_entity`.

    Examples
    --------
    >>> from ansys.dpf import core as dpf

    >>> # Instantiate operator
    >>> op = dpf.operators.min_max.time_of_min_by_entity()

    >>> # Make input connections
    >>> my_fields_container = dpf.FieldsContainer()
    >>> op.inputs.fields_container.connect(my_fields_container)
    >>> my_abs_value = bool()
    >>> op.inputs.abs_value.connect(my_abs_value)
    >>> my_compute_amplitude = bool()
    >>> op.inputs.compute_amplitude.connect(my_compute_amplitude)

    >>> # Instantiate operator and connect inputs in one line
    >>> op = dpf.operators.min_max.time_of_min_by_entity(
    ...     fields_container=my_fields_container,
    ...     abs_value=my_abs_value,
    ...     compute_amplitude=my_compute_amplitude,
    ... )

    >>> # Get output data
    >>> result_fields_container = op.outputs.fields_container()
    """

    def __init__(
        self,
        fields_container=None,
        abs_value=None,
        compute_amplitude=None,
        config=None,
        server=None,
    ):
        super().__init__(
            name="time_of_min_by_entity",
            config=config,
            server=server,
            inputs_type=InputsTimeOfMinByEntity,
            outputs_type=OutputsTimeOfMinByEntity,
        )
        if fields_container is not None:
            self.inputs.fields_container.connect(fields_container)
        if abs_value is not None:
            self.inputs.abs_value.connect(abs_value)
        if compute_amplitude is not None:
            self.inputs.compute_amplitude.connect(compute_amplitude)

    @staticmethod
    def _spec() -> Specification:
        description = r"""Thin wrapper around ``min_max_over_time_by_entity`` that exposes only
the time or frequency value at which each per-entity, per-component
minimum occurred.

The result forwarded on output pin 0 is pin 2 of
``min_max_over_time_by_entity`` (the ``time_freq_of_min`` fields
container). It is populated only when the input carries a time-frequency
support.

**When to use:** you only need the time of minimum. Prefer
``min_max_over_time_by_entity`` directly when you also need the extremum
value or the maximum-side outputs.
"""
        spec = Specification(
            description=description,
            map_input_pin_spec={
                0: PinSpecification(
                    name="fields_container",
                    type_names=["fields_container"],
                    optional=False,
                    document=r"""Fields container aggregated per entity across all time or frequency steps. Must expose the `time` label; otherwise the input is forwarded unchanged by the underlying operator.""",
                ),
                3: PinSpecification(
                    name="abs_value",
                    type_names=["bool"],
                    optional=True,
                    document=r"""When `true`, absolute values of the field entries are used before the min is computed. Default: `false`.""",
                ),
                4: PinSpecification(
                    name="compute_amplitude",
                    type_names=["bool"],
                    optional=True,
                    document=r"""When `true` and the input fields container has the `complex` label, the amplitude of the complex values is used before the min is computed. Ignored otherwise. Default: `false`.""",
                ),
            },
            map_output_pin_spec={
                0: PinSpecification(
                    name="fields_container",
                    type_names=["fields_container"],
                    optional=False,
                    document=r"""Time or frequency at which each per-entity, per-component minimum occurred. Populated only when the input carries a time-frequency support. Same shape as pin 2 of `min_max_over_time_by_entity`.""",
                ),
            },
        )
        return spec

    @staticmethod
    def default_config(server: AnyServerType = None) -> Config:
        """Returns the default config of the operator.

        This config can then be changed to the user needs and be used to
        instantiate the operator. The Configuration allows to customize
        how the operation will be processed by the operator.

        Parameters
        ----------
        server:
            Server with channel connected to the remote or local instance. When
            ``None``, attempts to use the global server.

        Returns
        -------
        config:
            A new Config instance equivalent to the default config for this operator.
        """
        return Operator.default_config(name="time_of_min_by_entity", server=server)

    @property
    def inputs(self) -> InputsTimeOfMinByEntity:
        """Enables to connect inputs to the operator

        Returns
        --------
        inputs:
            An instance of InputsTimeOfMinByEntity.
        """
        return self._inputs

    @property
    def outputs(self) -> OutputsTimeOfMinByEntity:
        """Enables to get outputs of the operator by evaluating it

        Returns
        --------
        outputs:
            An instance of OutputsTimeOfMinByEntity.
        """
        return self._outputs


class InputsTimeOfMinByEntity(_Inputs):
    """Intermediate class used to connect user inputs to
    time_of_min_by_entity operator.

    Examples
    --------
    >>> from ansys.dpf import core as dpf
    >>> op = dpf.operators.min_max.time_of_min_by_entity()
    >>> my_fields_container = dpf.FieldsContainer()
    >>> op.inputs.fields_container.connect(my_fields_container)
    >>> my_abs_value = bool()
    >>> op.inputs.abs_value.connect(my_abs_value)
    >>> my_compute_amplitude = bool()
    >>> op.inputs.compute_amplitude.connect(my_compute_amplitude)
    """

    def __init__(self, op: Operator):
        super().__init__(time_of_min_by_entity._spec().inputs, op)
        self._fields_container: Input[FieldsContainer] = Input(
            time_of_min_by_entity._spec().input_pin(0), 0, op, -1
        )
        self._inputs.append(self._fields_container)
        self._abs_value: Input[bool] = Input(
            time_of_min_by_entity._spec().input_pin(3), 3, op, -1
        )
        self._inputs.append(self._abs_value)
        self._compute_amplitude: Input[bool] = Input(
            time_of_min_by_entity._spec().input_pin(4), 4, op, -1
        )
        self._inputs.append(self._compute_amplitude)

    @property
    def fields_container(self) -> Input[FieldsContainer]:
        r"""Allows to connect fields_container input to the operator.

        Fields container aggregated per entity across all time or frequency steps. Must expose the `time` label; otherwise the input is forwarded unchanged by the underlying operator.

        Returns
        -------
        input:
            An Input instance for this pin.

        Examples
        --------
        >>> from ansys.dpf import core as dpf
        >>> op = dpf.operators.min_max.time_of_min_by_entity()
        >>> op.inputs.fields_container.connect(my_fields_container)
        >>> # or
        >>> op.inputs.fields_container(my_fields_container)
        """
        return self._fields_container

    @property
    def abs_value(self) -> Input[bool]:
        r"""Allows to connect abs_value input to the operator.

        When `true`, absolute values of the field entries are used before the min is computed. Default: `false`.

        Returns
        -------
        input:
            An Input instance for this pin.

        Examples
        --------
        >>> from ansys.dpf import core as dpf
        >>> op = dpf.operators.min_max.time_of_min_by_entity()
        >>> op.inputs.abs_value.connect(my_abs_value)
        >>> # or
        >>> op.inputs.abs_value(my_abs_value)
        """
        return self._abs_value

    @property
    def compute_amplitude(self) -> Input[bool]:
        r"""Allows to connect compute_amplitude input to the operator.

        When `true` and the input fields container has the `complex` label, the amplitude of the complex values is used before the min is computed. Ignored otherwise. Default: `false`.

        Returns
        -------
        input:
            An Input instance for this pin.

        Examples
        --------
        >>> from ansys.dpf import core as dpf
        >>> op = dpf.operators.min_max.time_of_min_by_entity()
        >>> op.inputs.compute_amplitude.connect(my_compute_amplitude)
        >>> # or
        >>> op.inputs.compute_amplitude(my_compute_amplitude)
        """
        return self._compute_amplitude


class OutputsTimeOfMinByEntity(_Outputs):
    """Intermediate class used to get outputs from
    time_of_min_by_entity operator.

    Examples
    --------
    >>> from ansys.dpf import core as dpf
    >>> op = dpf.operators.min_max.time_of_min_by_entity()
    >>> # Connect inputs : op.inputs. ...
    >>> result_fields_container = op.outputs.fields_container()
    """

    def __init__(self, op: Operator):
        super().__init__(time_of_min_by_entity._spec().outputs, op)
        self._fields_container: Output[FieldsContainer] = Output(
            time_of_min_by_entity._spec().output_pin(0), 0, op
        )
        self._outputs.append(self._fields_container)

    @property
    def fields_container(self) -> Output[FieldsContainer]:
        r"""Allows to get fields_container output of the operator

        Time or frequency at which each per-entity, per-component minimum occurred. Populated only when the input carries a time-frequency support. Same shape as pin 2 of `min_max_over_time_by_entity`.

        Returns
        -------
        output:
            An Output instance for this pin.

        Examples
        --------
        >>> from ansys.dpf import core as dpf
        >>> op = dpf.operators.min_max.time_of_min_by_entity()
        >>> # Get the output from op.outputs. ...
        >>> result_fields_container = op.outputs.fields_container()
        """
        return self._fields_container
