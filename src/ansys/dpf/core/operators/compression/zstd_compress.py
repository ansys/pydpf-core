"""
zstd_compress

Autogenerated DPF operator classes.
"""

from __future__ import annotations
from typing import TYPE_CHECKING

from warnings import warn
from ansys.dpf.core.dpf_operator import Operator
from ansys.dpf.core.inputs import Input, _Inputs
from ansys.dpf.core.outputs import Output, _Outputs
from ansys.dpf.core.operators.specification import PinSpecification, Specification
from ansys.dpf.core.config import Config
from ansys.dpf.core.server_types import AnyServerType

if TYPE_CHECKING:
    from ansys.dpf.core.custom_type_field import CustomTypeField
    from ansys.dpf.core.field import Field
    from ansys.dpf.core.generic_data_container import GenericDataContainer


class zstd_compress(Operator):
    r"""Compresses the data of a field with ZSTD compression algorithm.


    Inputs
    ------
    input_field: Field or CustomTypeField
        Field or custom type field of type "double" to compress.
    zstd_level: int, optional
        Compression level : from 0 to 20. Default = 3.
         High compression level increase computation time.
    num_threads: int, optional
        Number of threads for parallelization : from 0 to 20. Default = 4.

    Outputs
    -------
    compressed_output: GenericDataContainer
        Generic data container containing the following elements needed for decompression :
        - Uncompressed metadata (header, support, field definition)
        - Compressed data
        - Compressed scoping and its location
        - Compressed data pointer if it exists
        - Input field type (field or custom type field)

    output_bytes_size: int
        Total size of compressed data in bytes.

    Examples
    --------
    >>> from ansys.dpf import core as dpf

    >>> # Instantiate operator
    >>> op = dpf.operators.compression.zstd_compress()

    >>> # Make input connections
    >>> my_input_field = dpf.Field()
    >>> op.inputs.input_field.connect(my_input_field)
    >>> my_zstd_level = int()
    >>> op.inputs.zstd_level.connect(my_zstd_level)
    >>> my_num_threads = int()
    >>> op.inputs.num_threads.connect(my_num_threads)

    >>> # Instantiate operator and connect inputs in one line
    >>> op = dpf.operators.compression.zstd_compress(
    ...     input_field=my_input_field,
    ...     zstd_level=my_zstd_level,
    ...     num_threads=my_num_threads,
    ... )

    >>> # Get output data
    >>> result_compressed_output = op.outputs.compressed_output()
    >>> result_output_bytes_size = op.outputs.output_bytes_size()
    """

    def __init__(
        self,
        input_field=None,
        zstd_level=None,
        num_threads=None,
        config=None,
        server=None,
    ):
        super().__init__(
            name="zstd_compress",
            config=config,
            server=server,
            inputs_type=InputsZstdCompress,
            outputs_type=OutputsZstdCompress,
        )
        if input_field is not None:
            self.inputs.input_field.connect(input_field)
        if zstd_level is not None:
            self.inputs.zstd_level.connect(zstd_level)
        if num_threads is not None:
            self.inputs.num_threads.connect(num_threads)

    @staticmethod
    def _spec() -> Specification:
        description = r"""Compresses the data of a field with ZSTD compression algorithm.
"""
        spec = Specification(
            description=description,
            map_input_pin_spec={
                0: PinSpecification(
                    name="input_field",
                    type_names=["field", "custom_type_field"],
                    optional=False,
                    document=r"""Field or custom type field of type "double" to compress.""",
                ),
                2: PinSpecification(
                    name="zstd_level",
                    type_names=["int32"],
                    optional=True,
                    document=r"""Compression level : from 0 to 20. Default = 3.
 High compression level increase computation time.""",
                ),
                3: PinSpecification(
                    name="num_threads",
                    type_names=["int32"],
                    optional=True,
                    document=r"""Number of threads for parallelization : from 0 to 20. Default = 4.""",
                ),
            },
            map_output_pin_spec={
                0: PinSpecification(
                    name="compressed_output",
                    type_names=["generic_data_container"],
                    optional=False,
                    document=r"""Generic data container containing the following elements needed for decompression :
- Uncompressed metadata (header, support, field definition)
- Compressed data
- Compressed scoping and its location
- Compressed data pointer if it exists
- Input field type (field or custom type field)
""",
                ),
                1: PinSpecification(
                    name="output_bytes_size",
                    type_names=["uint64"],
                    optional=False,
                    document=r"""Total size of compressed data in bytes.""",
                ),
            },
        )
        return spec

    @staticmethod
    def default_config(server: AnyServerType = None) -> Config:
        """Returns the default config of the operator.

        This config can then be changed to the user needs and be used to
        instantiate the operator. The Configuration allows to customize
        how the operation will be processed by the operator.

        Parameters
        ----------
        server:
            Server with channel connected to the remote or local instance. When
            ``None``, attempts to use the global server.

        Returns
        -------
        config:
            A new Config instance equivalent to the default config for this operator.
        """
        return Operator.default_config(name="zstd_compress", server=server)

    @property
    def inputs(self) -> InputsZstdCompress:
        """Enables to connect inputs to the operator

        Returns
        --------
        inputs:
            An instance of InputsZstdCompress.
        """
        return self._inputs

    @property
    def outputs(self) -> OutputsZstdCompress:
        """Enables to get outputs of the operator by evaluating it

        Returns
        --------
        outputs:
            An instance of OutputsZstdCompress.
        """
        return self._outputs


class InputsZstdCompress(_Inputs):
    """Intermediate class used to connect user inputs to
    zstd_compress operator.

    Examples
    --------
    >>> from ansys.dpf import core as dpf
    >>> op = dpf.operators.compression.zstd_compress()
    >>> my_input_field = dpf.Field()
    >>> op.inputs.input_field.connect(my_input_field)
    >>> my_zstd_level = int()
    >>> op.inputs.zstd_level.connect(my_zstd_level)
    >>> my_num_threads = int()
    >>> op.inputs.num_threads.connect(my_num_threads)
    """

    def __init__(self, op: Operator):
        super().__init__(zstd_compress._spec().inputs, op)
        self._input_field: Input[Field | CustomTypeField] = Input(
            zstd_compress._spec().input_pin(0), 0, op, -1
        )
        self._inputs.append(self._input_field)
        self._zstd_level: Input[int] = Input(
            zstd_compress._spec().input_pin(2), 2, op, -1
        )
        self._inputs.append(self._zstd_level)
        self._num_threads: Input[int] = Input(
            zstd_compress._spec().input_pin(3), 3, op, -1
        )
        self._inputs.append(self._num_threads)

    @property
    def input_field(self) -> Input[Field | CustomTypeField]:
        r"""Allows to connect input_field input to the operator.

        Field or custom type field of type "double" to compress.

        Returns
        -------
        input:
            An Input instance for this pin.

        Examples
        --------
        >>> from ansys.dpf import core as dpf
        >>> op = dpf.operators.compression.zstd_compress()
        >>> op.inputs.input_field.connect(my_input_field)
        >>> # or
        >>> op.inputs.input_field(my_input_field)
        """
        return self._input_field

    @property
    def zstd_level(self) -> Input[int]:
        r"""Allows to connect zstd_level input to the operator.

        Compression level : from 0 to 20. Default = 3.
         High compression level increase computation time.

        Returns
        -------
        input:
            An Input instance for this pin.

        Examples
        --------
        >>> from ansys.dpf import core as dpf
        >>> op = dpf.operators.compression.zstd_compress()
        >>> op.inputs.zstd_level.connect(my_zstd_level)
        >>> # or
        >>> op.inputs.zstd_level(my_zstd_level)
        """
        return self._zstd_level

    @property
    def num_threads(self) -> Input[int]:
        r"""Allows to connect num_threads input to the operator.

        Number of threads for parallelization : from 0 to 20. Default = 4.

        Returns
        -------
        input:
            An Input instance for this pin.

        Examples
        --------
        >>> from ansys.dpf import core as dpf
        >>> op = dpf.operators.compression.zstd_compress()
        >>> op.inputs.num_threads.connect(my_num_threads)
        >>> # or
        >>> op.inputs.num_threads(my_num_threads)
        """
        return self._num_threads


class OutputsZstdCompress(_Outputs):
    """Intermediate class used to get outputs from
    zstd_compress operator.

    Examples
    --------
    >>> from ansys.dpf import core as dpf
    >>> op = dpf.operators.compression.zstd_compress()
    >>> # Connect inputs : op.inputs. ...
    >>> result_compressed_output = op.outputs.compressed_output()
    >>> result_output_bytes_size = op.outputs.output_bytes_size()
    """

    def __init__(self, op: Operator):
        super().__init__(zstd_compress._spec().outputs, op)
        self._compressed_output: Output[GenericDataContainer] = Output(
            zstd_compress._spec().output_pin(0), 0, op
        )
        self._outputs.append(self._compressed_output)
        self._output_bytes_size: Output[int] = Output(
            zstd_compress._spec().output_pin(1), 1, op
        )
        self._outputs.append(self._output_bytes_size)

    @property
    def compressed_output(self) -> Output[GenericDataContainer]:
        r"""Allows to get compressed_output output of the operator

        Generic data container containing the following elements needed for decompression :
        - Uncompressed metadata (header, support, field definition)
        - Compressed data
        - Compressed scoping and its location
        - Compressed data pointer if it exists
        - Input field type (field or custom type field)


        Returns
        -------
        output:
            An Output instance for this pin.

        Examples
        --------
        >>> from ansys.dpf import core as dpf
        >>> op = dpf.operators.compression.zstd_compress()
        >>> # Get the output from op.outputs. ...
        >>> result_compressed_output = op.outputs.compressed_output()
        """
        return self._compressed_output

    @property
    def output_bytes_size(self) -> Output[int]:
        r"""Allows to get output_bytes_size output of the operator

        Total size of compressed data in bytes.

        Returns
        -------
        output:
            An Output instance for this pin.

        Examples
        --------
        >>> from ansys.dpf import core as dpf
        >>> op = dpf.operators.compression.zstd_compress()
        >>> # Get the output from op.outputs. ...
        >>> result_output_bytes_size = op.outputs.output_bytes_size()
        """
        return self._output_bytes_size
