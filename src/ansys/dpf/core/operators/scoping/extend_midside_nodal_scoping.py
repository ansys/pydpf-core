"""
extend_midside_nodal_scoping

Autogenerated DPF operator classes.
"""

from __future__ import annotations
from typing import TYPE_CHECKING

from warnings import warn
from ansys.dpf.core.dpf_operator import Operator
from ansys.dpf.core.inputs import Input, _Inputs
from ansys.dpf.core.outputs import Output, _Outputs
from ansys.dpf.core.operators.specification import PinSpecification, Specification
from ansys.dpf.core.config import Config
from ansys.dpf.core.server_types import AnyServerType

if TYPE_CHECKING:
    from ansys.dpf.core.meshed_region import MeshedRegion
    from ansys.dpf.core.scoping import Scoping


class extend_midside_nodal_scoping(Operator):
    r"""Extends the input nodal scoping with the neighbor corner nodes of every
    midside node in the input. For each midside node in the scoping, the two
    corner nodes that bound it on the element edge are added to the output
    scoping.


    Inputs
    ------
    nodal_mesh_scoping: Scoping
        Nodal mesh scoping. Must have nodal location, otherwise an error is thrown.
    mesh: MeshedRegion
        Mesh region used to determine element connectivity and identify midside nodes.
    add_new_properties_to_mesh: bool, optional
        If true, the new mesh properties that are computed (like reversed connectivities) are kept in the mesh. Default is false.

    Outputs
    -------
    nodal_mesh_scoping: Scoping
        Extended nodal scoping containing all original nodes plus corner nodes neighboring any midside nodes.

    Examples
    --------
    >>> from ansys.dpf import core as dpf

    >>> # Instantiate operator
    >>> op = dpf.operators.scoping.extend_midside_nodal_scoping()

    >>> # Make input connections
    >>> my_nodal_mesh_scoping = dpf.Scoping()
    >>> op.inputs.nodal_mesh_scoping.connect(my_nodal_mesh_scoping)
    >>> my_mesh = dpf.MeshedRegion()
    >>> op.inputs.mesh.connect(my_mesh)
    >>> my_add_new_properties_to_mesh = bool()
    >>> op.inputs.add_new_properties_to_mesh.connect(my_add_new_properties_to_mesh)

    >>> # Instantiate operator and connect inputs in one line
    >>> op = dpf.operators.scoping.extend_midside_nodal_scoping(
    ...     nodal_mesh_scoping=my_nodal_mesh_scoping,
    ...     mesh=my_mesh,
    ...     add_new_properties_to_mesh=my_add_new_properties_to_mesh,
    ... )

    >>> # Get output data
    >>> result_nodal_mesh_scoping = op.outputs.nodal_mesh_scoping()
    """

    def __init__(
        self,
        nodal_mesh_scoping=None,
        mesh=None,
        add_new_properties_to_mesh=None,
        config=None,
        server=None,
    ):
        super().__init__(
            name="extend_midside_nodal_scoping",
            config=config,
            server=server,
            inputs_type=InputsExtendMidsideNodalScoping,
            outputs_type=OutputsExtendMidsideNodalScoping,
        )
        if nodal_mesh_scoping is not None:
            self.inputs.nodal_mesh_scoping.connect(nodal_mesh_scoping)
        if mesh is not None:
            self.inputs.mesh.connect(mesh)
        if add_new_properties_to_mesh is not None:
            self.inputs.add_new_properties_to_mesh.connect(add_new_properties_to_mesh)

    @staticmethod
    def _spec() -> Specification:
        description = r"""Extends the input nodal scoping with the neighbor corner nodes of every
midside node in the input. For each midside node in the scoping, the two
corner nodes that bound it on the element edge are added to the output
scoping.
"""
        spec = Specification(
            description=description,
            map_input_pin_spec={
                0: PinSpecification(
                    name="nodal_mesh_scoping",
                    type_names=["scoping"],
                    optional=False,
                    document=r"""Nodal mesh scoping. Must have nodal location, otherwise an error is thrown.""",
                ),
                1: PinSpecification(
                    name="mesh",
                    type_names=["meshed_region"],
                    optional=False,
                    document=r"""Mesh region used to determine element connectivity and identify midside nodes.""",
                ),
                200: PinSpecification(
                    name="add_new_properties_to_mesh",
                    type_names=["bool"],
                    optional=True,
                    document=r"""If true, the new mesh properties that are computed (like reversed connectivities) are kept in the mesh. Default is false.""",
                ),
            },
            map_output_pin_spec={
                0: PinSpecification(
                    name="nodal_mesh_scoping",
                    type_names=["scoping"],
                    optional=False,
                    document=r"""Extended nodal scoping containing all original nodes plus corner nodes neighboring any midside nodes.""",
                ),
            },
        )
        return spec

    @staticmethod
    def default_config(server: AnyServerType = None) -> Config:
        """Returns the default config of the operator.

        This config can then be changed to the user needs and be used to
        instantiate the operator. The Configuration allows to customize
        how the operation will be processed by the operator.

        Parameters
        ----------
        server:
            Server with channel connected to the remote or local instance. When
            ``None``, attempts to use the global server.

        Returns
        -------
        config:
            A new Config instance equivalent to the default config for this operator.
        """
        return Operator.default_config(
            name="extend_midside_nodal_scoping", server=server
        )

    @property
    def inputs(self) -> InputsExtendMidsideNodalScoping:
        """Enables to connect inputs to the operator

        Returns
        --------
        inputs:
            An instance of InputsExtendMidsideNodalScoping.
        """
        return self._inputs

    @property
    def outputs(self) -> OutputsExtendMidsideNodalScoping:
        """Enables to get outputs of the operator by evaluating it

        Returns
        --------
        outputs:
            An instance of OutputsExtendMidsideNodalScoping.
        """
        return self._outputs


class InputsExtendMidsideNodalScoping(_Inputs):
    """Intermediate class used to connect user inputs to
    extend_midside_nodal_scoping operator.

    Examples
    --------
    >>> from ansys.dpf import core as dpf
    >>> op = dpf.operators.scoping.extend_midside_nodal_scoping()
    >>> my_nodal_mesh_scoping = dpf.Scoping()
    >>> op.inputs.nodal_mesh_scoping.connect(my_nodal_mesh_scoping)
    >>> my_mesh = dpf.MeshedRegion()
    >>> op.inputs.mesh.connect(my_mesh)
    >>> my_add_new_properties_to_mesh = bool()
    >>> op.inputs.add_new_properties_to_mesh.connect(my_add_new_properties_to_mesh)
    """

    def __init__(self, op: Operator):
        super().__init__(extend_midside_nodal_scoping._spec().inputs, op)
        self._nodal_mesh_scoping: Input[Scoping] = Input(
            extend_midside_nodal_scoping._spec().input_pin(0), 0, op, -1
        )
        self._inputs.append(self._nodal_mesh_scoping)
        self._mesh: Input[MeshedRegion] = Input(
            extend_midside_nodal_scoping._spec().input_pin(1), 1, op, -1
        )
        self._inputs.append(self._mesh)
        self._add_new_properties_to_mesh: Input[bool] = Input(
            extend_midside_nodal_scoping._spec().input_pin(200), 200, op, -1
        )
        self._inputs.append(self._add_new_properties_to_mesh)

    @property
    def nodal_mesh_scoping(self) -> Input[Scoping]:
        r"""Allows to connect nodal_mesh_scoping input to the operator.

        Nodal mesh scoping. Must have nodal location, otherwise an error is thrown.

        Returns
        -------
        input:
            An Input instance for this pin.

        Examples
        --------
        >>> from ansys.dpf import core as dpf
        >>> op = dpf.operators.scoping.extend_midside_nodal_scoping()
        >>> op.inputs.nodal_mesh_scoping.connect(my_nodal_mesh_scoping)
        >>> # or
        >>> op.inputs.nodal_mesh_scoping(my_nodal_mesh_scoping)
        """
        return self._nodal_mesh_scoping

    @property
    def mesh(self) -> Input[MeshedRegion]:
        r"""Allows to connect mesh input to the operator.

        Mesh region used to determine element connectivity and identify midside nodes.

        Returns
        -------
        input:
            An Input instance for this pin.

        Examples
        --------
        >>> from ansys.dpf import core as dpf
        >>> op = dpf.operators.scoping.extend_midside_nodal_scoping()
        >>> op.inputs.mesh.connect(my_mesh)
        >>> # or
        >>> op.inputs.mesh(my_mesh)
        """
        return self._mesh

    @property
    def add_new_properties_to_mesh(self) -> Input[bool]:
        r"""Allows to connect add_new_properties_to_mesh input to the operator.

        If true, the new mesh properties that are computed (like reversed connectivities) are kept in the mesh. Default is false.

        Returns
        -------
        input:
            An Input instance for this pin.

        Examples
        --------
        >>> from ansys.dpf import core as dpf
        >>> op = dpf.operators.scoping.extend_midside_nodal_scoping()
        >>> op.inputs.add_new_properties_to_mesh.connect(my_add_new_properties_to_mesh)
        >>> # or
        >>> op.inputs.add_new_properties_to_mesh(my_add_new_properties_to_mesh)
        """
        return self._add_new_properties_to_mesh


class OutputsExtendMidsideNodalScoping(_Outputs):
    """Intermediate class used to get outputs from
    extend_midside_nodal_scoping operator.

    Examples
    --------
    >>> from ansys.dpf import core as dpf
    >>> op = dpf.operators.scoping.extend_midside_nodal_scoping()
    >>> # Connect inputs : op.inputs. ...
    >>> result_nodal_mesh_scoping = op.outputs.nodal_mesh_scoping()
    """

    def __init__(self, op: Operator):
        super().__init__(extend_midside_nodal_scoping._spec().outputs, op)
        self._nodal_mesh_scoping: Output[Scoping] = Output(
            extend_midside_nodal_scoping._spec().output_pin(0), 0, op
        )
        self._outputs.append(self._nodal_mesh_scoping)

    @property
    def nodal_mesh_scoping(self) -> Output[Scoping]:
        r"""Allows to get nodal_mesh_scoping output of the operator

        Extended nodal scoping containing all original nodes plus corner nodes neighboring any midside nodes.

        Returns
        -------
        output:
            An Output instance for this pin.

        Examples
        --------
        >>> from ansys.dpf import core as dpf
        >>> op = dpf.operators.scoping.extend_midside_nodal_scoping()
        >>> # Get the output from op.outputs. ...
        >>> result_nodal_mesh_scoping = op.outputs.nodal_mesh_scoping()
        """
        return self._nodal_mesh_scoping
