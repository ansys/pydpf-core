"""Version for ansys-dpf-core"""
# major, minor, patch
version_info = 0, 2, 1

# Nice string for the version
__version__ = '.'.join(map(str, version_info))
__ansys_version__ = "211"
max_server_version = "1.0"