.. code::

    git clone https://github.com/ansys/pydpf-core
    cd pydpf-core
    pip install -e .

.. note::

    Alternatively, if you have `uv`_ installed, you can create the virtual environment and
    install PyDPF-Core in editable mode in a single step by running ``uv sync`` instead.